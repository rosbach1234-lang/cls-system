const http=require('http');const fs=require('fs');const path=require('path');const p=process.env.PORT||8080;http.createServer((q,r)=>{const u=q.url==='/'||q.url==='/index.html'?'index.html':q.url==='/platform'||q.url==='/platform.html'?'platform.html':null;if(!u){r.writeHead(404);r.end('Not found');return;}let f=path.join(__dirname,u);fs.readFile(f,(e,d)=>{r.writeHead(e?404:200,{'Content-Type':'text/html'});r.end(e?'Not found':d);});}).listen(p,()=>console.log('OK '+p));
